/**
 * @deprecated Use '@/features/expenses' instead.
 * Preserved for backwards compatibility during incremental domain extraction.
 */
export {
  calculateNetBalances,
  computeDebtSettlements,
} from '@/features/expenses';
